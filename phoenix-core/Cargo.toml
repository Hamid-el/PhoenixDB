[package]
name = "phoenix-core"
version = "0.1.0"
edition = "2021"
authors = ["Hamid"]
description = "PhoenixDB core engine — embedded relational database"

[dependencies]
thiserror = "2"
log = "0.4"

[dev-dependencies]
tempfile = "3"
env_logger = "0.11"
